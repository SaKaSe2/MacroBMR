<Window x:Class="MacroBMR.VirtualCursorOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="VirtualCursorOverlay"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowState="Maximized" WindowStartupLocation="Manual"
        IsHitTestVisible="False" Focusable="False">
    <Grid Background="Transparent" IsHitTestVisible="False">
        <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False">
            <!-- Ripple Effect Container (Clicks/Taps) -->
            <Canvas x:Name="RippleCanvas" IsHitTestVisible="False"/>

            <!-- Virtual Cursor Container -->
            <Canvas x:Name="CursorContainer" Width="60" Height="60" Canvas.Left="-100" Canvas.Top="-100" IsHitTestVisible="False">
                <!-- Cursor Glow Circle -->
                <Ellipse Width="24" Height="24" Canvas.Left="0" Canvas.Top="0" Fill="#334F46E5">
                    <Ellipse.Effect>
                        <BlurEffect Radius="8"/>
                    </Ellipse.Effect>
                </Ellipse>

                <!-- Virtual Cursor Pointer Icon -->
                <Path Data="M 0,0 L 0,16 L 4.5,12.5 L 8,18 L 10.5,16.5 L 7,11 L 12.5,11 Z"
                      Fill="#EF4444" Stroke="#FFFFFF" StrokeThickness="1.5"
                      Canvas.Left="2" Canvas.Top="2">
                    <Path.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.4" BlurRadius="4" ShadowDepth="1"/>
                    </Path.Effect>
                </Path>

                <!-- Mini Action / Step Badge -->
                <Border x:Name="StepBadge" Background="#1E293B" CornerRadius="8" Padding="5,1"
                        Canvas.Left="16" Canvas.Top="12" Opacity="0.95" BorderBrush="#E2E8F0" BorderThickness="0.8">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.3" BlurRadius="3" ShadowDepth="1"/>
                    </Border.Effect>
                    <TextBlock x:Name="TxtStep" Text="1" Foreground="#FFFFFF" FontSize="9" FontWeight="Bold"/>
                </Border>
            </Canvas>
        </Canvas>
    </Grid>
</Window>
